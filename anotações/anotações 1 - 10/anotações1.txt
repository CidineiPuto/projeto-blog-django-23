Primeiramente vamos criar um repositório git.

vamos iniciar o git;

git init

Depois vamos adicionar ele, com;

git remote add origin repositório

Depois vamos o iniciar
git push -u origin main

Depois disso, podemos usar apenas
git push

Para publicar.

Mas agora, vamos criar o .gitignore.
Pode procurar no google

.gitignore django

Ou pode pegar da aula.